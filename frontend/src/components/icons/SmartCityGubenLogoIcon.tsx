import Icon from "@/assets/icons/smart-city-guben-logo.svg?react";
import { LucideProps } from "lucide-react";

export const SmartCityGubenLogoIcon = (props: LucideProps) => {
  return <Icon {...props} />;
}
